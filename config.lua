Config = {
    commandName = "dvall",
    playerace = "staff.dvall",
    restricCommand = true, --- Settings this to false will enable all players to be able to utilise dvall!
    notifySystem = 1, -- 1 for codem-notify, 2 for t-notify, 3 for okok-notify, 4 for mythic-notify
}
